1. gh repo create name
   git ignore node

2. set up all files in directory

3. npm init -y

4. npm install --save-dev @babel/core @babel/preset-env postcss autoprefixer browser-sync cssnano gulp gulp-babel gulp-postcss sass gulp-sass gulp-terser

5. open two terminals and run these commands one in each
	> gulp
	> sass --watch src/scss/style.scss:src/css/style.css

6. initial commit